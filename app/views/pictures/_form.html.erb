<%= content_tag :div, :id => "form_"+f.object_id.to_s do %>

    <%= f.label :image %>
    <%= f.file_field :image %>
    <%= link_to_remove_fields "remove", f, "count_id", "form_#{f.object_id}" %>

<% end %>
