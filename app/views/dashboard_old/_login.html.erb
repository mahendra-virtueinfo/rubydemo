<%= form_for(:user_session, :url => url_for(:controller => controller.controller_name, :action => controller.action_name)) do |f| %>

<%= f.label :login %>
<%= f.text_field :login %>

<%= f.label :password %>
<%= f.password_field :password %>

<%= f.submit "SIGN IN" %>

<% end %>
